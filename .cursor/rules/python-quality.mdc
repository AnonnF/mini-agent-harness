---
description: Python 文件的工程质量、类型、测试和异常处理规范
globs:
  - "**/*.py"
alwaysApply: false
---

# Python 工程质量规则

处理 Python 文件时遵守以下规范：

- 使用完整且有意义的类型标注。
- 命名必须表达业务含义。
- 避免没有必要的全局变量。
- 不捕获宽泛异常后静默忽略。
- 对外部输入进行验证。
- 为网络、文件系统和工具执行设计失败路径。
- 核心业务逻辑应方便单元测试。
- 避免把 I/O 和纯业务计算混在同一个函数中。
- 不记录 API Key、Token 或敏感配置。
- 资源应被正确释放。
- 异步函数中避免阻塞式 I/O。
- 每个模块保持清晰、有限的职责。
- 除非有实际需要，不创建复杂继承体系或设计模式。

每完成一个功能，都要考虑：

- 正常路径
- 边界情况
- 失败路径
- 安全风险
- 回归风险

优先让我根据测试场景自己实现测试，不要直接生成全部测试代码。

常用质量检查命令：

    pytest
    ruff check .
    ruff format --check .
    mypy src